#ifndef _UTILS_H_ 
#define _UTILS_H_
	
	#include "../Graph/Node.h"
	#include <list>
	using namespace std;
	bool distanceComparator(Node* d1, Node* d2);
	bool esUsable(Node& g, int p);
	
	Node* getClosestPoint(int potions, int bagCapacity, Node* n, list<Node*>& gyms, list<Node*>& pokeparadas);
	Node* getFirstUsableGym(list<Node*>& gyms, int potions);
#endif
